import { AcheronTooltipDirective } from './acheron-tooltip.directive';

describe('AcheronTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new AcheronTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
